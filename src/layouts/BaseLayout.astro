---
import brandManifest from '../../config/brand_manifest.json';
import systemArchitecture from '../../system_architecture.json';
import uxDesignToken from '../../ux_design_token.json';
import '@astrouxds/tokens/dist/css/index.css';

interface Props {
  title: string;
  description: string;
  canonicalUrl: string;
}

const { title = '', description = '', canonicalUrl = '' } = Astro.props;
const brandName = systemArchitecture.tenant_settings.brand_name;
const tokens = uxDesignToken.design_system.tokens;
const safeTitle = title || '';
const safeDescription = description || '';
const safeCanonicalUrl = canonicalUrl || Astro.url.href;
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
  <title>{safeTitle} | {brandName}</title>
  <meta name="description" content={safeDescription} />
  <link rel="canonical" href={safeCanonicalUrl} />

  <!-- OpenGraph Tags compliant with Naver / B2B Authority -->
  <meta property="og:site_name" content={brandName} />
  <meta property="og:title" content={safeTitle.slice(0, 40)} />
  <meta property="og:description" content={safeDescription.slice(0, 80)} />
  <meta property="og:type" content="website" />
  <meta property="og:url" content={safeCanonicalUrl} />
  <meta property="og:image" content={new URL('/og-image.webp', safeCanonicalUrl).href} />
  
  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={safeTitle.slice(0, 40)} />
  <meta name="twitter:description" content={safeDescription.slice(0, 80)} />
  <meta name="twitter:image" content={new URL('/og-image.webp', safeCanonicalUrl).href} />

  <!-- Favicon -->
  <link rel="icon" type="image/png" href="/favicon.png" />

  <!-- Verification Tags -->
  <meta name="naver-site-verification" content="verified_bizkitgrow" />

  <!-- Google Fonts Inter -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet" />



  <!-- Dynamic JSON-LD Schema Injection Slot -->
  <slot name="schema" />

  <style define:vars={{
    fontFamily: tokens.typography.family,
    headingFamily: tokens.typography.heading_family
  }}>
    body {
      font-family: var(--fontFamily);
    }
    h1, h2, h3, h4, h5, h6 {
      font-family: var(--headingFamily);
      letter-spacing: -0.02em;
    }
  </style>
</head>
<body class="antialiased selection:bg-interactive-hover selection:text-text-inverse bg-background-base text-text-primary min-h-screen flex flex-col">
  <header class="border-b border-border-subtle sticky top-0 bg-background-surface/80 backdrop-blur-md z-50">
    <div class="max-w-[1280px] mx-auto px-6 h-16 flex items-center justify-between">
      <a href="/" class="text-xl font-extrabold tracking-tight text-text-primary flex items-center gap-2 hover:opacity-90">
        <span class="w-8 h-8 rounded-lg bg-interactive-normal flex items-center justify-center text-text-inverse text-base">b</span>
        {brandName}
      </a>
      
      <!-- Desktop Nav -->
      <nav class="hidden md:flex items-center gap-8 text-sm font-medium">
        <a href="/" class={`transition-colors ${Astro.url.pathname === '/' ? 'text-text-primary font-bold' : 'text-text-secondary hover:text-text-primary'}`}>Platform</a>
        <a href="/esim/united-states" class={`transition-colors ${Astro.url.pathname.startsWith('/esim') ? 'text-text-primary font-bold' : 'text-text-secondary hover:text-text-primary'}`}>Connectivity</a>
        <a href="/reputation/new-york" class={`transition-colors ${Astro.url.pathname.startsWith('/reputation') ? 'text-text-primary font-bold' : 'text-text-secondary hover:text-text-primary'}`}>Reputation</a>
        <a href="/solutions/crm-system" class={`transition-colors ${Astro.url.pathname.startsWith('/solutions') ? 'text-text-primary font-bold' : 'text-text-secondary hover:text-text-primary'}`}>Solutions</a>
        <a href="/blog" class={`transition-colors ${Astro.url.pathname.startsWith('/blog') ? 'text-text-primary font-bold' : 'text-text-secondary hover:text-text-primary'}`}>Intelligence</a>
      </nav>
      
      <div class="flex items-center gap-4">
        <a href="#waitlist" class="hidden sm:inline-flex h-9 px-4 items-center justify-center rounded-lg bg-interactive-normal hover:bg-interactive-hover text-text-inverse text-xs font-semibold tracking-wide transition-colors">
          Initialize System
        </a>
        
        <!-- Mobile Menu Toggle -->
        <button id="mobile-menu-btn" class="md:hidden p-2 text-text-primary">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="4" x2="20" y1="12" y2="12"/><line x1="4" x2="20" y1="6" y2="6"/><line x1="4" x2="20" y1="18" y2="18"/></svg>
        </button>
      </div>
    </div>
    
    <!-- Mobile Nav -->
    <div id="mobile-nav" class="hidden md:hidden border-t border-border-subtle bg-background-surface px-6 py-4 space-y-4">
      <a href="/" class={`block transition-colors ${Astro.url.pathname === '/' ? 'text-text-primary font-bold' : 'text-text-secondary hover:text-text-primary'}`}>Platform</a>
      <a href="/esim/united-states" class={`block transition-colors ${Astro.url.pathname.startsWith('/esim') ? 'text-text-primary font-bold' : 'text-text-secondary hover:text-text-primary'}`}>Connectivity</a>
      <a href="/reputation/new-york" class={`block transition-colors ${Astro.url.pathname.startsWith('/reputation') ? 'text-text-primary font-bold' : 'text-text-secondary hover:text-text-primary'}`}>Reputation</a>
      <a href="/solutions/crm-system" class={`block transition-colors ${Astro.url.pathname.startsWith('/solutions') ? 'text-text-primary font-bold' : 'text-text-secondary hover:text-text-primary'}`}>Solutions</a>
      <a href="/blog" class={`block transition-colors ${Astro.url.pathname.startsWith('/blog') ? 'text-text-primary font-bold' : 'text-text-secondary hover:text-text-primary'}`}>Intelligence</a>
      <a href="#waitlist" class="block w-full h-9 px-4 mt-4 text-center rounded-lg bg-interactive-normal text-text-inverse text-xs font-semibold leading-9 tracking-wide">
        Initialize System
      </a>
    </div>
  </header>

  <script is:inline>
    // Simple mobile menu toggle logic
    document.addEventListener('DOMContentLoaded', () => {
      const btn = document.getElementById('mobile-menu-btn');
      const nav = document.getElementById('mobile-nav');
      if (btn && nav) {
        btn.addEventListener('click', () => {
          nav.classList.toggle('hidden');
        });
      }
    });
  </script>

  <!-- Main View Frame -->
  <div class="flex-grow">
    <slot />
  </div>

  <!-- Corporate Footer -->
  <footer class="border-t border-border-subtle bg-background-surface py-12 mt-20">
    <div class="max-w-[1280px] mx-auto px-6 flex flex-col md:flex-row items-center justify-between gap-6">
      <div class="text-sm text-text-secondary">
        © {new Date().getFullYear()} {systemArchitecture.tenant_settings.parent_company_copyright}. All operational assets secure.
      </div>
      <div class="flex gap-6 text-xs text-text-secondary">
        <a href="/terms" class="hover:underline text-text-primary">Terms of Authority</a>
        <a href="/privacy" class="hover:underline text-text-primary">Privacy Perimeter</a>
        <a href={`mailto:${systemArchitecture.tenant_settings.support_email}`} class="hover:underline text-text-primary">Operations Support</a>
      </div>
    </div>
  </footer>

  <!-- GTM Lazy Hydration Optimization -->
  <script is:inline>
    window.addEventListener('load', () => {
      if ('requestIdleCallback' in window) {
        requestIdleCallback(() => {
          // Lazy init GTM Container
          console.log('[Telemetry] Lazy-hydrating GTM trackers...');
        });
      } else {
        setTimeout(() => {
          console.log('[Telemetry] Hydrating GTM trackers...');
        }, 3000);
      }
    });
  </script>

  <!-- Lenis Smooth Scroll Engine -->
  <script>
    import Lenis from 'lenis';
    const lenis = new Lenis({
      duration: 1.2,
      easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
      infinite: false,
    });

    function raf(time: number) {
      lenis.raf(time);
      requestAnimationFrame(raf);
    }
    requestAnimationFrame(raf);
  </script>
</body>
</html>
